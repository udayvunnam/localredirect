import type { Metadata } from 'next';
import { Geist, Geist_Mono } from 'next/font/google';
import './globals.css';

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
});

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
});

export const metadata: Metadata = {
  title: 'Redirect Service - Localhost & OAuth Development',
  description:
    'Minimal HTTPS redirect service for Vercel. Perfect for OAuth callbacks and redirecting to localhost during development.',
  keywords: ['redirect', 'oauth', 'localhost', 'development', 'vercel'],
  openGraph: {
    title: 'Redirect Service',
    description: 'Minimal HTTPS redirect service for OAuth and local development',
    type: 'website',
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>{children}</body>
    </html>
  );
}
